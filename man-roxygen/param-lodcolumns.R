#' @param lodcolumns In a \code{scanone} or equivalent object, this
#' parameter indicates which LOD columns to consider. In other objects
#' with LOD-column-associated elements, this parameter indicates which
#' of those LOD-column-associated elements to consider. The specified
#' LOD columns must be a character vector of LOD column names, a logical
#' vector with length equal to the number of LOD columns, or a numeric
#' vector of indices \emph{with respect to the set of LOD columns}. If
#' no LOD columns are specified, all are used.
